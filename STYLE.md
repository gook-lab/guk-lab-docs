# 톤앤매너 — 이 저장소의 글쓰기 가이드

여기 올라가는 글의 목소리를 정해둔 문서입니다. 문서를 새로 쓰거나 고치기 전에
한 번 읽고 시작해 주세요. 에이전트도 마찬가지입니다 — `CLAUDE.md`가 이 문서를
가리킵니다.

## 한 문장으로

**5~6년차 프론트엔드 엔지니어가 읽는 사람을 존중하며 자기 프로젝트를
소개하는 목소리.**

전문성은 내용(실측, 근거, 경로)이 담당하고, 문장은 정중하되 딱딱하지 않게
풀어 씁니다. 단정적 문어체로 쓴다고 더 전문적으로 보이지는 않습니다.

## 어미 — 습니다체

- 본문은 **습니다체**로 씁니다. "~했습니다 / ~입니다 / ~기능입니다 /
  ~설계했습니다".
- 반말 단정체("~한다", "~않는다", "~하라")와 캐주얼한 해요체("~해요",
  "~했어요")는 본문에서 피합니다.
- 명령·금지는 경험 기반 권유로 바꿉니다.
  - ❌ "파이프 뒤에서 종료코드를 읽지 마라."
  - ⭕ "파이프 뒤에서 종료코드를 읽으면 실패가 0으로 보입니다. 이걸로
    게이트가 조용히 무력화된 적이 있습니다."
- 독자에게 권할 때는 "~해 주세요 / ~하는 것이 좋습니다"를 씁니다.

## 명사형으로 쓰는 곳 — 헤드 요약과 표 셀

- **제목 옆 한 줄 요약, 목차의 "한 줄" 컬럼, 표 안의 셀, GitHub About 설명**은
  문장이 아니라 **명사형 요약**으로 씁니다. 헤드 자리에서 "~습니다"로 끝나는
  문장은 오히려 어색합니다.
  - ⭕ "뱀파이어 서바이버즈풍 불릿헤븐 로그라이트 — 순수 JS + PixiJS"
  - ⭕ "죽은 링크·없는 경로·헤더 없는 표 검출"
  - ❌ "죽은 링크를 잡아요" / "…검출합니다"
- 코드 블록과 주석도 짧은 서술로 써도 됩니다.
- 에이전트 지시문(`harness/skills/*/SKILL.md`, 커맨드 프롬프트) — 지시체를
  유지합니다. 기계가 읽는 글은 짧고 명확한 것이 우선입니다.
- 남의 글이나 규칙을 **인용**할 때는 원문 그대로 둡니다. 인용문은 톤 수정
  대상이 아닙니다.

## 헤딩 — 스캔 가능한 기술 명사구

헤딩은 목차와 스크롤에서 **혼자 읽혀도 무엇을 다루는지 알 수 있는 기술
명사구**로 씁니다. 형식: **설계 대상 — 방식/결정** (대시 뒤는 선택).

- ❌ 구호·은유형: "좌표계는 셋, 변환 지점은 하나", "이 게임의 심장",
  "콘텐츠는 데이터입니다"
- ❌ 질문형: "왜 라이브러리를 두 번 호출하는가"
- ❌ 인용 슬로건형: "결승선 = \"손맛을 평가할 수 있다\""
- ⭕ "공통 좌표계 — 타입 3계층 · 변환 유틸 일원화"
- ⭕ "타이밍 코어 — 타일 각도→박자 파생 공식"
- ⭕ "이중 라이브러리 호출 — 교차 검증"

구호가 설계 의도를 잘 담고 있다면 버리지 말고 **본문 첫 문장으로**
내립니다. 헤딩은 찾는 자리, 본문은 설득하는 자리입니다. 목표를 담고 싶으면
괄호로 붙입니다 — "주말 1 마일스톤 — 코어 판정 루프 (목표: 손맛 평가 가능)".

2026-08-25에 3d-map·rhythm-godot 등 7개 레포의 구호형 헤딩 20여 개를 이
기준으로 일괄 교정했습니다.

## 바꾸지 않는 것

톤은 바꿔도 **근거는 바꾸지 않습니다.** 이 저장소의 원칙(근거 없는 문장
금지)은 그대로입니다.

- 수치·날짜·측정 시점·경로·링크는 그대로 유지합니다.
- 실패담은 숨기지 않고 1인칭으로 적습니다. "반나절을 날렸습니다" 같은
  문장이 이 문서들의 가장 좋은 부분입니다.
- 그럴듯한 일반론으로 채우지 않습니다. 겪은 일만 적습니다.

## 프로젝트 서술 — 기능보다 판단이 보이게

프로젝트 문서는 기능 목록만으로 끝내지 않습니다. 읽는 사람이 **무엇을 만들었는지,
어떤 문제가 있었는지, 왜 그 해결책을 골랐는지, 무엇으로 확인했는지**를 순서대로
따라갈 수 있어야 합니다.

기본 골격은 다음과 같습니다. 모든 문서에 같은 헤딩을 강제하는 템플릿이 아니라,
빠진 맥락을 확인하는 편집 기준입니다.

1. **한 줄 소개** — 대상 사용자·핵심 기능·차별점 중 중요한 두 가지
2. **프로젝트 소개** — 무엇을 왜 만들었는지
3. **진행 방식** — 책임 경계·데이터 흐름·검증 순서
4. **주요 기능** — 기능명과 사용자가 얻는 결과
5. **문제와 해결** — 문제 → 원인 → 선택 → 검증 결과
6. **회고** — 배운 점뿐 아니라 남은 제약과 다음 개선

### 문제와 해결 — 해결책보다 앞에 근거

- 문제는 증상과 영향을 함께 적습니다. "성능 문제가 있었습니다"보다
  "5,000개 노드에서 줌이 36fps까지 떨어졌습니다"가 낫습니다.
- 처음 추측과 실제 원인이 달랐다면 둘 다 남깁니다. 판단이 바뀐 근거가 가장 좋은
  설명입니다.
- 해결에는 사용 기술보다 **왜 그 경계를 선택했는지**를 적습니다.
- 마지막에는 테스트·측정·회귀 게이트처럼 해결 여부를 확인한 방법을 붙입니다.

```text
문제: 여러 컴포넌트가 zoom을 구독해 5,000개 노드에서 36fps까지 떨어졌습니다.
원인: 저장 비용이 아니라 매 프레임 발생하는 React 전체 리렌더링이 병목이었습니다.
해결: 줌 값을 구독 경계 밖으로 옮겨 필요한 요소만 갱신했습니다.
검증: 같은 시나리오에서 106fps를 확인했습니다.
```

### 주요 기능 — 명사 목록에 사용자 결과를 붙이기

- ❌ "로그인, 지도, 타임라인, 캐시 구현"
- ⭕ "TTS 오디오를 IndexedDB에 저장해 한 번 들은 발음은 오프라인에서도
  재생됩니다."
- 기능이 많으면 화면이나 사용자 흐름으로 묶습니다. 라이브러리 이름은 구현 설명에
  필요할 때만 붙입니다.

### 회고 — 성장 서사보다 판단의 변화

"많이 배웠습니다"만으로 끝내지 않습니다. 시작할 때의 가정, 구현 뒤 바뀐 판단,
아직 남은 제약을 적습니다. 미완료 항목은 실패가 아니라 현재 경계를 정확하게
보여주는 정보입니다.

```text
자동 인식 범위를 먼저 정했지만, 공개 샘플에서 가구 재현율이 약 5%였습니다.
벽·방·개구부만 자동화하고 나머지는 사용자가 보정하게 범위를 바꿨습니다.
다음에는 후보별 신뢰도를 표시해 검토 순서를 안내하려 합니다.
```

## 매체별 밀도 — 사실은 같고 역할은 다르게

README·Notion·이력서·웹 포트폴리오는 같은 사실 원장을 사용하되 문장 밀도는
다르게 가져갑니다.

| 매체 | 먼저 보여줄 것 | 권장 밀도 |
|---|---|---|
| README | 실행·구조·검증 방법 | 개발자가 재현할 수 있는 경로와 명령까지 |
| Notion 상세 | 맥락·주요 기능·문제 해결·회고 | 문제마다 원인·선택·검증을 1~3문단으로 |
| 이력서 | 지원 직무와 가까운 역할·성과 | 한 불릿에 행동과 결과 하나씩 |
| 웹 포트폴리오 | 정체성과 대표 판단 | 짧은 문장과 화면 중심, 상세 페이지로 연결 |

매체에 맞게 줄여도 수치·기간·기술·프로젝트 상태는 달라지면 안 됩니다. 원장이
충돌하면 더 그럴듯한 쪽을 고르지 않고 코드·Git·측정 기록에서 먼저 확인합니다.

## 어색한 에이전트 문장 — 주체·대상·결과 복원

다음 표현이 반복되면 문장이 기술적으로 보여도 실제 정보는 적습니다.

- "~을 기반으로 구축했습니다"
- "~을 진행했습니다"
- "복잡한 문제를 해결했습니다"
- "효율성과 확장성을 향상했습니다"
- "사용자 경험을 강화했습니다"

무조건 금지하지는 않지만, **누가 무엇을 어떻게 바꿨고 무엇이 달라졌는지**로
교체할 수 있는지 먼저 확인합니다.

- ❌ "React Query를 기반으로 서버 상태 관리 체계를 구축했습니다."
- ⭕ "저장 뒤 refetch를 기다리지 않고 이동해 이전 데이터가 보였습니다.
  `refetchQueries` 완료 뒤 화면을 전환하도록 순서를 바꿨습니다."
- ❌ "성능 최적화를 진행해 사용자 경험을 향상했습니다."
- ⭕ "탭을 지연 로딩해 초기 번들을 632KB에서 356KB로 줄였습니다."

한 문장에 추상 명사와 기술명을 여러 개 겹치지 않습니다. 기술은 주어가 아니라
문제를 해결한 수단입니다.

### 과장된 동사와 은유 — 실제 동작으로 교체

에이전트가 기술 문장을 강조하려고 다음 표현을 반복해서 사용하는 경우가 있습니다.

- `규칙으로 못박았습니다` / `문서에 박아뒀습니다`
- `테스트로 잠갔습니다` / `골든 데이터로 잠급니다`
- `의존성을 걷어냈습니다`
- `이 기능이 프로젝트의 심장·주인공입니다`
- `문서가 조용히 썩습니다`

이 표현들은 뜻을 짐작할 수 있지만, 실제로 한 작업을 직접 말하는 표현보다 강하고
비슷한 글에서 반복되면 에이전트 문체가 두드러집니다. 강조가 필요하지 않다면 다음처럼
바꿉니다.

| 피할 표현 | 구체적인 표현 |
|---|---|
| 기준을 못박았습니다 | 기준을 문서에 명시했습니다 |
| 골든 테스트로 잠갔습니다 | 골든 테스트로 변경 여부를 검사합니다 |
| 회귀 테스트로 박아뒀습니다 | 해당 사례를 회귀 테스트에 추가했습니다 |
| 의존성을 걷어냈습니다 | 의존성을 제거했습니다 / 경계를 분리했습니다 |
| 프로젝트의 심장입니다 | 결과를 결정하는 핵심 계산 단계입니다 |
| 문서가 조용히 썩습니다 | 원본 변경이 반영되지 않아 문서가 오래된 상태로 남습니다 |

`조용히`는 오류·경고 없이 실패하거나 값이 바뀌는 **관찰 가능한 동작**을 설명할
때만 사용합니다. 이때도 무엇이 표시되지 않는지 함께 적습니다.

- ❌ "캐시가 조용히 깨집니다."
- ⭕ "query key의 자료형이 달라도 오류가 발생하지 않아 이전 캐시가 화면에
  남았습니다."

`못박다·박아두다·잠그다·걷어내다`를 금칙어처럼 기계적으로 치환하지는 않습니다.
인용이나 실제 UI 문구가 아니라면, 먼저 `명시·추가·검사·제거·분리`처럼 코드와
문서에서 확인할 수 있는 동사로 바꿀 수 있는지 검토합니다.

### 반복되는 논설형 문장 — 결론보다 사실을 먼저

에이전트 문서에는 아래 문장 구조가 반복되는 경향이 있습니다.

- `A가 아니라 B입니다`
- `핵심은 …입니다` / `문제는 …입니다` / `중요한 건 …입니다`
- `결국 …하는 것입니다`
- `반드시·절대·전부·유일한`

모두 사용할 수 있는 표현이지만, 문단마다 쓰면 모든 내용을 반전이나 결론처럼
보이게 합니다. 실제 오해를 바로잡는 경우가 아니라면 B를 직접 말하고, 결론형 도입
대신 관찰한 증상·바꾼 대상·확인 결과부터 적습니다. 절대어는 예외가 없음을 코드나
정책으로 확인할 수 있을 때만 사용합니다.

- ❌ "중요한 건 캐시가 아니라 경계를 지키는 것입니다."
- ⭕ "서버 상태는 React Query에 두고, 입력 중인 폼 값은 컴포넌트 상태에
  유지했습니다."
- ❌ "이 규칙은 반드시 모든 요청을 막습니다."
- ⭕ "CI에서 허용 목록 밖의 외부 요청이 추가되면 빌드를 실패시킵니다."

`경계·계약·게이트·불변식·SSOT·드리프트·폴백·파이프라인` 같은 말은 정확한 기술
용어일 수 있으므로 금지하지 않습니다. 다만 추상 명사를 여러 개 이어 붙이지 않고,
어떤 모듈·규칙·검사가 그 역할을 하는지 이름을 붙입니다. 문단마다 굵은 표어를 하나씩
만들거나 `~것입니다`, `~하도록 했습니다`로 같은 어미를 반복하는 것도 피합니다.

긴 문서는 아래 표현을 검색해 반복과 모호한 문맥을 점검합니다. 이것은 금칙어 목록이
아니라 편집이 필요한 후보를 찾는 검색 목록입니다.

```text
핵심은 | 중요한 건 | 문제는 | 결국
단순히 | 단순한 | 그냥 | 이게 | 이건 | 가 아니라
것입니다 | 할 수 있습니다 | 하도록 했습니다
구성했습니다 | 구축했습니다 | 진행했습니다 | 처리했습니다 | 관리합니다 | 보장합니다
일관되게 | 명시적으로 | 구조화 | 표면화
파이프라인 | 불변식 | SSOT | 경계 | 게이트 | 드리프트 | 폴백
무해하게 | 책임으로 | 책임 경계 | 실질적 난이도
유일한 | 전부 | 반드시 | 절대
```

- `이게·이건`은 선행 대상이 불분명하면 실제 대상 이름으로 바꿉니다.
- `단순히·그냥`이 앞선 작업이나 대안을 낮추는 강조로만 쓰였다면 삭제합니다.
- `구성·구축·진행·처리·관리`는 대상과 변화가 함께 있을 때는 사용할 수 있습니다.
- `보장`은 테스트·타입·정책이 책임지는 범위를 밝힐 수 있을 때만 씁니다.
- `명시적으로·일관되게·구조화·표면화`는 무엇이 어디서 어떻게 달라졌는지를 바로
  설명하지 못하면 구체적인 동사로 교체합니다.
- `무해하게·실질적 난이도·책임으로` 같은 평가는 조건·영향·담당 범위로 풀어 씁니다.

## 피하는 것

- 이모지 남발, 느낌표 남발, 억지 유행어. 신뢰감은 어미와 근거에서 나와야지,
  과장에서 나오면 금방 무너집니다.
- "혁신적인", "강력한" 같은 홍보 문구. 여긴 포트폴리오이기 전에 기록입니다.
- 지나친 겸양("부족하지만…")도 반대 방향으로 어색합니다. 담백하게 씁니다.

## 적용 범위

이 가이드는 guk-lab-docs만이 아니라 **guk-lab 이름으로 밖에 보이는 글
전부**에 적용됩니다:

- 각 프로젝트 레포의 `README.md`와 `docs/`의 사람이 읽는 문서
- GitHub 레포의 About 설명 — 한국어 **명사형 한 줄 요약**으로 통일합니다
  ("FigJam 스타일 React 무한 캔버스 라이브러리 — 도형·리치 텍스트·커넥터·댓글").
  문장 어미("~요", "~습니다")로 끝내지 않고, 영·한 혼재도 피합니다
- 포트폴리오 사이트의 카피 (경력·이력서 파생 문구는 이력서 원장과의 동기화
  계약을 따릅니다 — 원장도 습니다체이므로 자연스럽게 일치합니다)

## 한 / 영 버전

- **README만 영문판을 함께 둡니다** — `README.md`(한국어) +
  `README.en.md`(영어), 양쪽 상단에 `한국어 · English` 스위처 링크.
  내부 문서(docs/)까지 미러링하면 사본이 반드시 낡기 때문에, 입구만
  이중화합니다.
- 영어도 같은 목소리로 씁니다 — 자연스러운 1인칭 서술, 홍보 문구 없이.
  수치·경로·표·코드는 한국어판과 동일하게 유지합니다.
- README를 고치면 **영문판도 같은 커밋에서** 고칩니다. 한쪽만 고친 채
  커밋하지 않습니다.

## 어미를 일괄 전환할 때 — 검증 패턴

2026-08-24에 문서 30여 개를 한 번에 전환하며 얻은 함정 목록입니다.

- **잔존 검사는 두 가지 grep이 필요합니다.** 줄 끝 패턴
  (`grep -nE '(요|죠)[.!?")]*$'`)만 돌리면 **표 셀 안의 어미**
  (`…잡아요 |`)가 전부 빠져나갑니다. 셀 내부 패턴
  (`grep -nE '(어요|아요|해요|예요|죠) *\|'`)을 같이 돌려 주세요.
- **어미만 기계적으로 바꾸면 동사 원형이 깨집니다.** 실제로 나온 오변환:
  켜다→"킵니다"(→켭니다), 돌리다→"돕니다"(→돌립니다),
  잠그다→"잠습니다"(→잠급니다), 흐르다→"흡니다"(→흐릅니다),
  가다→"가입니다"(→갑니다). 전환 후 이 목록을 grep 해서 잡아 주세요.
- **"~이예요"는 항상 틀린 표기입니다.** 받침 있는 말 + "이에요",
  받침 없는 말 + "예요". "아니예요"도 틀리고 "아니에요"가 맞습니다.
- 명사 "필요/개요/중요"는 "요"로 끝나도 어미가 아니므로 예외 처리합니다.

## 셀프 체크리스트

문서를 저장하기 전에 확인합니다:

- [ ] 본문이 "~했습니다 / ~입니다"로 끝나고 있습니까? ("~한다 / ~해요"가
  남아 있지 않습니까?)
- [ ] 헤딩이 혼자 읽혀도 무엇을 다루는지 알 수 있는 기술 명사구입니까?
  (구호·은유·질문형이 남아 있지 않습니까?)
- [ ] 수치 옆에 측정 시점이 그대로 붙어 있습니까?
- [ ] 이모지·과장 없이도 읽는 사람을 존중하는 톤입니까?
- [ ] README를 고쳤다면 README.en.md도 같이 고쳤습니까?
- [ ] 기능 목록만 있고 문제·판단·검증이 빠져 있지 않습니까?
- [ ] "구축·진행·향상"을 구체적인 행동과 결과로 바꿀 수 있습니까?
- [ ] "못박다·박아두다·잠그다·걷어내다·심장·주인공" 같은 강조 표현을
  실제 동작으로 바꿀 수 있습니까?
- [ ] `A가 아니라 B`, `핵심은`, `결국`, 절대어가 근거 없이 반복되지 않습니까?
- [ ] `경계·게이트·불변식` 같은 용어에 구체적인 대상과 검사 동작이 붙어 있습니까?
- [ ] 문제와 해결 사이에 실제 원인과 선택 근거가 있습니까?
- [ ] 회고에 남은 제약이나 다음 개선이 포함되어 있습니까?
- [ ] README·Notion·이력서의 수치·기간·기술·상태가 같은 원장과 일치합니까?
